# Template for forum implementation

## Requirements

To install the pip packages we need to run the following

````bash
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
````
## Running
````bash
py manage.py runserver
````
